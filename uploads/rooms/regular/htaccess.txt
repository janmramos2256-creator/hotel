# Protect room images directory
Options -Indexes

# Allow image files only
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Order allow,deny
    Allow from all
</FilesMatch>

# Deny everything else
<FilesMatch "^(?!.*\.(jpg|jpeg|png|gif|webp)$).*$">
    Order deny,allow
    Deny from all
</FilesMatch>